package com.sanosysalvos.geo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Clase principal de arranque para el microservicio de Mapeo Geográfico y Ubicaciones.
 */
@SpringBootApplication
public class MsGeolocalizacionApplication {
    public static void main(String[] args) {
        SpringApplication.run(MsGeolocalizacionApplication.class, args);
    }
}